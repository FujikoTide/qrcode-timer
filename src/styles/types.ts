/**
 * The size of the component
 * @default "md"
 */
export type Size = 'sm' | 'md' | 'lg'

/**
 * The style variation of the component
 * @default "primary"
 */
export type Intent = 'primary' | 'secondary' | 'danger' | 'warning' | 'ghost'

/**
 * The width of the component
 * @default "fit"
 */
export type Width = 'fit' | 'halfWidth' | 'fullWidth'
